[project]
name = "mdtools"
version = "0.2.0"
description = "Desktop workbench for MiniDisc and CD-R: design disc labels, J-cards and case inserts for Cricut cutting and printing, record albums from foobar2000 or straight from a CD, burn audio CD-Rs with CD-Text, and title MiniDiscs over infrared"
requires-python = ">=3.11"
dependencies = [
    "PySide6>=6.7",
    "Pillow>=10.0",
    "telethon>=1.34",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "pytest-qt>=4.4",
    "pyinstaller>=6.0",
]

[project.scripts]
mdtools = "mdtools.main:main"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
"mdtools.templates" = ["defaults.json"]
"mdtools.i18n" = ["*.qm"]
